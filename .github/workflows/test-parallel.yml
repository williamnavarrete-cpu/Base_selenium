name: 🧪 Tests Paralelos - Selenium + Behave + Allure

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    name: "🧪 Test: ${{ matrix.tag }}"
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        tag:
          - "@HU-2"
          - "@HU-3"

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: 📦 Instalar dependencias
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: ⚙️ Crear archivo .env
        run: |
          echo "EXECUTION_TYPE=manager" > .env
          echo "BROWSER=chrome" >> .env
          echo "URL=https://hakatools.hakalab.com" >> .env
          echo "TIME_IMPLICITY=20" >> .env
          echo "EXECUTOR_NAME=GitHub Actions" >> .env
          echo "EXECUTOR_BUILD_NAME=Run #${{ github.run_number }}" >> .env
          echo "EXECUTOR_TYPE=github" >> .env

      - name: 🚀 Ejecutar escenarios con tag ${{ matrix.tag }}
        run: |
          behave \
            -f allure_behave.formatter:AllureFormatter \
            -o allure-results \
            --tags="${{ matrix.tag }}" \
            --no-capture \
            --no-skipped \
            ./features
        continue-on-error: true

      - name: 📤 Subir resultados Allure
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results-${{ strategy.job-index }}
          path: allure-results/
          retention-days: 5

  report:
    name: "📊 Generar Reporte Allure"
    runs-on: ubuntu-latest
    needs: test
    if: always()

    steps:
      - name: 📥 Descargar resultados de todos los jobs
        uses: actions/download-artifact@v4
        with:
          pattern: allure-results-*
          path: allure-results
          merge-multiple: true

      - name: 🔍 Verificar resultados descargados
        run: |
          echo "Archivos descargados:"
          find allure-results -name "*-result.json" | wc -l
          echo "JSONs:"
          ls allure-results/*-result.json 2>/dev/null || echo "Sin resultados"

      - name: 📊 Generar reporte Allure
        uses: simple-elf/allure-report-action@master
        with:
          allure_results: allure-results
          allure_report: allure-report
          keep_reports: 1

      - name: 🌐 Publicar en GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-report
          force_orphan: true
